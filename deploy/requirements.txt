# ArionComply runtime dependencies.
#
# Derived from actual imports in rag/, api_server.py, and enrichment/,
# with versions pinned to what's known-working on the dev host.
#
# Regenerated 2026-08-31 during Ship 101'.a on-prem PoC deployment
# after the prior `pip freeze` output was found to contain 57+ lines
# of dev-host Ubuntu apt-package leaks (CUDA, cloud-init, dbus, etc).
#
# When adding a new runtime import, add it here explicitly.

# --- Web framework ---
fastapi==0.140.0
uvicorn==0.46.0
python-multipart==0.0.27
pydantic==2.13.3

# --- LLM ---
openai==2.48.0

# --- Graph orchestration + session persistence ---
langgraph==1.1.10
langgraph-checkpoint-postgres==3.0.5

# --- Vector store + graph DB ---
chromadb==1.5.9
neo4j==6.2.0
rank-bm25==0.2.2

# --- Postgres drivers ---
# psycopg v3 (used by langgraph's PostgresSaver + AsyncPostgresSaver)
psycopg[binary]==3.3.4
# psycopg2 (used by non-langgraph write paths)
psycopg2-binary==2.9.12

# --- Numerics ---
numpy==2.4.4

# --- Document intake (uploads / templates) ---
python-docx==1.2.0
mammoth==1.12.0
openpyxl==3.1.5
pdfplumber==0.11.9

# --- Utilities ---
python-dotenv==1.2.2
PyYAML==6.0.1
python-dateutil==2.8.2
requests==2.34.2
httpx==0.28.1
typing-extensions==4.15.0
