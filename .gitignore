# Secrets
.env
*.key
*.pem

# Ship 111'.d — PoC-side deployment log (per-customer, never committed)
.deployment_log.jsonl

# Python
__pycache__/
*.pyc
*.pyo
.Python
venv/
.venv/

# Data - don't commit large files or DB data
chromadb_data/
chroma_db/
*.db
*.sqlite

# Ship 103'.a moved these into Git LFS. Left as commented reference
# so we remember why they were briefly gitignored.
# db/baseline/chroma_prebuilt.tar.gz
# db/baseline/chroma_prebuilt.meta.json

# Results - keep CSVs but not large outputs
results/eval_*.csv

# Archive - inactive code kept locally, not tracked
archive/

# OS
.DS_Store

# Claude Code runtime locks (per-session)
.claude/scheduled_tasks.lock
private/
.phoenix/
