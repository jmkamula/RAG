# Secrets
.env
*.key
*.pem

# Python
__pycache__/
*.pyc
*.pyo
.Python
venv/
.venv/

# Data - don't commit large files or DB data
chromadb_data/
chroma_db/
*.db
*.sqlite

# Ship 102' golden images kept locally until we ship an image-repo
# (Ship 103'). GitHub's 100 MB per-file limit blocks the Chroma tar.
db/baseline/chroma_prebuilt.tar.gz
db/baseline/chroma_prebuilt.meta.json

# Results - keep CSVs but not large outputs
results/eval_*.csv

# Archive - inactive code kept locally, not tracked
archive/

# OS
.DS_Store

# Claude Code runtime locks (per-session)
.claude/scheduled_tasks.lock
private/
.phoenix/
