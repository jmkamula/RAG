slug: records_of_processing
title: "Records of Processing Activities (RoPA)"
description: |
  GDPR Art.30 records of processing — the always-current inventory
  of processing operations that a supervisory authority can demand
  on 24-hour notice. Requires the controller-facing register (30.1)
  and, for processors, the processor-facing register (30.2).
primary_framework: GDPR:2016/679
display_order: 60
auditor_expects: |
  The RoPA is the FIRST document a supervisory authority requests
  in almost any investigation. Auditors check: (1) it exists in
  writing (30.4); (2) it covers all Art.30(1) mandatory fields per
  processing activity; (3) it's kept current (annual review at
  minimum); (4) data-flow mapping is coherent — where PII actually
  lives ties to entries in the RoPA.

leaves:
  - leaf_id: req:Art.30:records_of_processing
    role: primary_register
    workflow_order: 2
    role_note: |
      The RoPA itself. Auditor first-look artefact. Common
      failures: missing Art.30(1)(f) retention periods, missing
      Art.30(1)(g) technical + organisational measures reference,
      stale (last updated > 12 months ago).

  - leaf_id: req:Art.30:ropa_maintenance_procedure
    role: primary_procedure
    workflow_order: 1
    role_note: |
      When to update the RoPA — new processing operation, purpose
      change, transfer route change, retention-period change,
      third-party engagement.

  - leaf_id: req:Art.30:data_flow_inventory
    role: supporting_prerequisite
    workflow_order: 0
    role_note: |
      Where does each PII category actually live — systems,
      backups, third parties. Prerequisite for a complete RoPA;
      auditors triangulate the RoPA against this inventory.

  - leaf_id: req:Art.30:ropa_annual_review
    role: review_record
    workflow_order: 3

  # ISO 27001 program layer — PII protection policy is the
  # foundational program that RoPA describes the operations of.
  - leaf_id: req:A.5.34:privacy_and_pii_protection_policy
    role: supporting_iso_mirror
    workflow_order: 4
    role_note: |
      ISO 27001 program layer. The PII protection policy defines
      the RoPA's scope + governance. Auditors look for the RoPA
      being coherent with this policy.

  # ISO 27701 controller extension — the PIMS transfer register
  # is the same shape as the Art.30 records but scoped to
  # cross-jurisdiction PII movement.
  - leaf_id: req:A.7.5.1:transfer_basis_procedure
    role: supporting_iso_mirror
    workflow_order: 5
    role_note: |
      ISO 27701 controller extension. Basis for PII transfer
      between jurisdictions — mandated Art.30(1)(e) content:
      transfers to third countries + safeguards.

  - leaf_id: req:A.7.5.3:transfer_records_procedure
    role: supporting_iso_mirror
    workflow_order: 6
    role_note: |
      ISO 27701 controller extension. Records of PII transfer —
      the operational record that backs Art.30(1)(e) claims.

  # ISO 27701 processor extension — processor's own RoPA
  # (mirrors Art.30(2)).
  - leaf_id: req:B.8.2.6:processor_ropa_register
    role: supporting_iso_mirror
    workflow_order: 7
    role_note: |
      ISO 27701 processor extension. Processor-side RoPA covering
      Art.30(2). When the tenant is a processor as well as a
      controller, both registers exist alongside.
