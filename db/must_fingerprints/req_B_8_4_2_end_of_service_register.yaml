# Per-MUST fingerprint catalog for req:B.8.4.2:end_of_service_register
# Auto-generated skeleton — review and tighten before commit.
#
# Used by rag/intake/leaf_driven_scan.py to back-bind existing
# approved findings to specific MUSTs they semantically satisfy
# but weren't tagged with at extraction time.

schema_version: 1
target_evidence_requirement: "req:B.8.4.2:end_of_service_register"
target_control: "B.8.4.2"
target_standard: "ISO27701:2019"

must_fingerprints:
  - must_id: "item:B.8.4.2:reg_certification_ref"
    description: "Certification reference issued to customer"
    excerpt_keywords:
      - [certification, ref, return]
      - [certification, reference, return]
      - [certification, reference, issued, return]
      - [reference, issued, return]
      - [reference, issued, customer, return]
      - [issued, customer, return]
      - [per, row, return]
      - [each, row, return]

  - must_id: "item:B.8.4.2:reg_completion_date"
    description: "Completion date per row (return delivered / disposal complete)"
    excerpt_keywords:
      - [completion, date, return]
      - [completion, date, return]
      - [date, return]
      - [date, return, delivered]
      - [return, delivered]
      - [return, delivered, disposal]
      - [delivered, disposal, return]
      - [delivered, disposal, complete, return]

  - must_id: "item:B.8.4.2:reg_action_type"
    description: "Action type per row (return / transfer / dispose / hybrid)"
    excerpt_keywords:
      - [action, return]
      - [action, return, transfer]
      - [return, transfer]
      - [return, transfer, dispose]
      - [transfer, dispose]
      - [transfer, dispose, hybrid]
      - [dispose, hybrid, return]
      - [per, row, return]

  - must_id: "item:B.8.4.2:reg_termination_date"
    description: "Contract termination date per row"
    excerpt_keywords:
      - [termination, date, return]
      - [contract, termination, return]
      - [contract, termination, date, return]
      - [per, row, return]
      - [each, row, return]
      - [column, containing, return]
      - [each, entry, return]
      - [column, header, return]

  - must_id: "item:B.8.4.2:reg_customer_id"
    description: "Customer identifier per row"
    excerpt_keywords:
      - [customer, id, return]
      - [customer, identifier, return]
      - [per, row, return]
      - [each, row, return]
      - [column, containing, return]
      - [each, entry, return]
      - [column, header, return]

