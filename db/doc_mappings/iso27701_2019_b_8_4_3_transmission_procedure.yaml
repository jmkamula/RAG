# Generated by scripts/generate_doc_mappings.py
# Per-leaf scaffold — covers the case where a tenant uploads a dedicated
# document for this specific leaf. Hand-authored umbrella YAMLs (e.g.
# access_control_policy.yaml covering A.5.15-18) coexist; both can match
# the same upload, and the discovery layer unions their target_leaves.

schema_version: 1
mapping_id: doc.ISO27701_2019.B_8_4_3.transmission_procedure

filename_fingerprints:
  - tokens: [transmission, controls, procedure]
  - tokens: [transmission, controls, proc]
  - tokens: [processor, transmission, controls]
  - tokens: [processor, transmission]

target_leaves:
  - leaf_id: "req:B.8.4.3:transmission_procedure"
    control_ref: "B.8.4.3"
    role: procedure

confidence_weights:
  filename: 0.6
  body: 0.3
  explicit_refs: 0.1
