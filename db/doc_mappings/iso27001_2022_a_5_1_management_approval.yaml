# Generated by scripts/generate_doc_mappings.py
# Per-leaf scaffold — covers the case where a tenant uploads a dedicated
# document for this specific leaf. Hand-authored umbrella YAMLs (e.g.
# access_control_policy.yaml covering A.5.15-18) coexist; both can match
# the same upload, and the discovery layer unions their target_leaves.

schema_version: 1
mapping_id: doc.ISO27001_2022.A_5_1.management_approval

filename_fingerprints:
  - tokens: [approval, infosec, policy]
  - tokens: [approval, infosec, pol]
  - tokens: [top, management, approval]
  - tokens: [top, mgmt, approval]
  - tokens: [top, management]
  - tokens: [top, mgmt]

target_leaves:
  - leaf_id: "req:A.5.1:management_approval"
    control_ref: "A.5.1"
    role: approval

confidence_weights:
  filename: 0.6
  body: 0.3
  explicit_refs: 0.1
