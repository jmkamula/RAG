# Generated by scripts/generate_doc_mappings.py
# Per-leaf scaffold — covers the case where a tenant uploads a dedicated
# document for this specific leaf. Hand-authored umbrella YAMLs (e.g.
# access_control_policy.yaml covering A.5.15-18) coexist; both can match
# the same upload, and the discovery layer unions their target_leaves.

schema_version: 1
mapping_id: doc.ISO27701_2019.B_8_2_3.marketing_use_procedure

filename_fingerprints:
  - tokens: [use, prohibition, procedure]
  - tokens: [use, prohibition, proc]
  - tokens: [marketing, advertising, use]
  - tokens: [marketing, advertising]

target_leaves:
  - leaf_id: "req:B.8.2.3:marketing_use_procedure"
    control_ref: "B.8.2.3"
    role: procedure

confidence_weights:
  filename: 0.6
  body: 0.3
  explicit_refs: 0.1
