# Generated by scripts/generate_register_yamls.py
# Tier 3 register sweep — bulk-generated scaffold. Per-leaf tuning may refine
# sheet fingerprints and column bindings as real tenant data lands.

schema_version: 1
mapping_id: workbook.ISO27001_2022.A_8_16.detection_register

sheet_name_fingerprints:
  - tokens: [use, case, register]
  - tokens: [use, case, reg]
  - tokens: [detection, use, case]
  - tokens: [detection, use]

header_row_hints: [1, 2]
min_data_rows: 1

passes:

  - pass_name: detection_register
    target_control: "A.8.16"
    target_evidence_requirement: "req:A.8.16:detection_register"
    target_evidence_type: register
    required_columns:
      - fingerprint: [use, case, id]
        binds_to: "item:A.8.16:reg_use_case_id"
      - fingerprint: [coverage]
        binds_to: "item:A.8.16:reg_coverage"
    optional_columns:
      - fingerprint: [threat, mapping]
        binds_to: "item:A.8.16:reg_threat_mapping"
        coverage: partial
      - fingerprint: [tp, rate]
        binds_to: "item:A.8.16:reg_tp_rate"
        coverage: partial
      - fingerprint: [last, tuned]
        binds_to: "item:A.8.16:reg_last_tuned"
        coverage: partial

confidence_weights:
  sheet_name: 0.5
  required_columns: 0.4
  row_count: 0.1
