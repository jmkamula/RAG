# Generated by scripts/generate_register_yamls.py
# Tier 3 register sweep — bulk-generated scaffold. Per-leaf tuning may refine
# sheet fingerprints and column bindings as real tenant data lands.

schema_version: 1
mapping_id: workbook.ISO27701_2019.A_7_2_3.consent_artifact_register

sheet_name_fingerprints:
  - tokens: [mechanism, artifact, register]
  - tokens: [mechanism, artifact, reg]
  - tokens: [consent, mechanism, artifact]
  - tokens: [consent, mechanism]

header_row_hints: [1, 2]
min_data_rows: 1

passes:

  - pass_name: consent_artifact_register
    target_control: "A.7.2.3"
    target_evidence_requirement: "req:A.7.2.3:consent_artifact_register"
    target_evidence_type: register
    freshness:
      column_fingerprint: [effective, date]
      alternative_fingerprints:
        - [date]
      days: 365
    required_columns:
      - fingerprint: [channel, id]
        binds_to: "item:A.7.2.3:reg_channel_id"
      - fingerprint: [artifact, snapshot]
        binds_to: "item:A.7.2.3:reg_artifact_snapshot"
    optional_columns:
      - fingerprint: [purpose, link]
        binds_to: "item:A.7.2.3:reg_purpose_link"
        coverage: partial
      - fingerprint: [effective, date]
        binds_to: "item:A.7.2.3:reg_effective_date"
        coverage: partial
      - fingerprint: [review, signoff]
        binds_to: "item:A.7.2.3:reg_review_signoff"
        coverage: partial

    # Ship 90'.a — LLM-proposed cite_columns (curator sweep).
    cite_columns:
      - fingerprint: [purpose, link]
        binds_to: "item:A.7.2.3:reg_purpose_link"
        cite_kind: internal_document
        verification_days: 365

confidence_weights:
  sheet_name: 0.5
  required_columns: 0.4
  row_count: 0.1
