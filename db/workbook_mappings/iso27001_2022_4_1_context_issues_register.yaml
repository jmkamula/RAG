# Generated by scripts/generate_register_yamls.py
# Tier 3 register sweep — bulk-generated scaffold. Per-leaf tuning may refine
# sheet fingerprints and column bindings as real tenant data lands.

schema_version: 1
mapping_id: workbook.ISO27001_2022.4_1.context_issues_register

sheet_name_fingerprints:
  - tokens: [external, issues, register]
  - tokens: [external, issues, reg]
  - tokens: [internal, external, issues]
  # Removed `[internal, external]` 2026-06-11: too broad — matched
  # stakeholder-map sheets ("Internal&External Parties") that are 4.2
  # interested-parties evidence, not 4.1 context-issues evidence.
  # The 4.1 leaf wants internal/external ISSUES; the parties sheet
  # has internal/external CATEGORIES of stakeholders. 4.2 mapping
  # handles those.
  - tokens: [context, issues]
  - tokens: [issues, register]

header_row_hints: [1, 2]
min_data_rows: 1

passes:

  - pass_name: context_issues_register
    target_control: "4.1"
    target_evidence_requirement: "req:4.1:context_issues_register"
    target_evidence_type: register
    freshness:
      column_fingerprint: [last, assessed]
      alternative_fingerprints:
        - [date]
      days: 365
    required_columns:
      - fingerprint: [internal, issues]
        binds_to: "item:4.1:internal_issues"
      - fingerprint: [external, issues]
        binds_to: "item:4.1:external_issues"
    optional_columns:
      - fingerprint: [relevance, to, ismsm]
        binds_to: "item:4.1:relevance_to_ismsm"
        coverage: partial
      - fingerprint: [owner]
        binds_to: "item:4.1:owner"
        coverage: partial
      - fingerprint: [last, assessed]
        binds_to: "item:4.1:reg_last_assessed"
        coverage: partial
      - fingerprint: [risk, handoff]
        binds_to: "item:4.1:reg_risk_handoff"
        coverage: partial

confidence_weights:
  sheet_name: 0.5
  required_columns: 0.4
  row_count: 0.1
