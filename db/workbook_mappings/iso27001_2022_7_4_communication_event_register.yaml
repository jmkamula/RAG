# Generated by scripts/generate_register_yamls.py
# Tier 3 register sweep — bulk-generated scaffold. Per-leaf tuning may refine
# sheet fingerprints and column bindings as real tenant data lands.

schema_version: 1
mapping_id: workbook.ISO27001_2022.7_4.communication_event_register

sheet_name_fingerprints:
  - tokens: [communication, event, register]
  - tokens: [communication, event, reg]
  - tokens: [communication, event]
  # Manual tuning: tenants commonly maintain a "Risk Comms Matrix" or
  # "Communications Plan" sheet listing what gets communicated to whom,
  # how, and how often — fits the 7.4 communication-event leaf even
  # though the title vocabulary differs.
  - tokens: [risk, comm]
  - tokens: [risk, communication]
  - tokens: [comm, matrix]
  - tokens: [communication, matrix]
  - tokens: [comm, plan]
  - tokens: [communication, plan]

header_row_hints: [1, 2]
min_data_rows: 1

passes:

  - pass_name: communication_event_register
    target_control: "7.4"
    target_evidence_requirement: "req:7.4:communication_event_register"
    target_evidence_type: register
    freshness:
      column_fingerprint: [date]
      alternative_fingerprints:
        - [date]
      days: 365
    required_columns:
      # The identifier — event / communication / risk / item id.
      # Matrix-style sheets (per-risk comms plan) and log-style sheets
      # (per-event history) both fit; tenants name the row-id column
      # after whatever their unit of communication is.
      - fingerprint: [event, id]
        binds_to: "item:7.4:reg_event_id"
      - fingerprint: [communication, id]
        binds_to: "item:7.4:reg_event_id"
      - fingerprint: [risk, id]
        binds_to: "item:7.4:reg_event_id"
      - fingerprint: [item, id]
        binds_to: "item:7.4:reg_event_id"
      # The "what is being communicated" — topic / subject / message /
      # description / risk-description (matrix-style).
      - fingerprint: [topic]
        binds_to: "item:7.4:reg_topic"
      - fingerprint: [subject]
        binds_to: "item:7.4:reg_topic"
      - fingerprint: [message]
        binds_to: "item:7.4:reg_topic"
      - fingerprint: [risk, description]
        binds_to: "item:7.4:reg_topic"
    # Full coverage by default — a real Interested Party column IS the
    # audience evidence, a real Comms Method column IS the channel.
    # Reserve `coverage: partial` for genuinely weak signals.
    optional_columns:
      - fingerprint: [audience]
        binds_to: "item:7.4:reg_audience"
      - fingerprint: [recipient]
        binds_to: "item:7.4:reg_audience"
      - fingerprint: [interested, party]
        binds_to: "item:7.4:reg_audience"
      - fingerprint: [stakeholder]
        binds_to: "item:7.4:reg_audience"
      - fingerprint: [channel]
        binds_to: "item:7.4:reg_channel"
      - fingerprint: [method]
        binds_to: "item:7.4:reg_channel"
      - fingerprint: [medium]
        binds_to: "item:7.4:reg_channel"
      - fingerprint: [frequency]
        binds_to: "item:7.4:reg_date"
      - fingerprint: [schedule]
        binds_to: "item:7.4:reg_date"
      - fingerprint: [date]
        binds_to: "item:7.4:reg_date"
      - fingerprint: [sender]
        binds_to: "item:7.4:reg_sender"

confidence_weights:
  sheet_name: 0.5
  required_columns: 0.4
  row_count: 0.1
